spring:
  application:
    name: user
  cloud:
    config:
      discovery:
        service-id: CONFIG-SERVER
        enabled: true
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/